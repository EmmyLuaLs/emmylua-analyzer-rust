//! code_style checkers (CodeStyleCheck family; disabled by default in `CheckConfig` via `is_code_default_enable`).

pub mod invert_if;
pub mod non_literal_expressions_in_assert;
pub mod preferred_local_alias;
